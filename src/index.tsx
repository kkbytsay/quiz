import * as React from "react";
import * as ReactDOM from "react-dom";
import { App } from "./App";
ReactDOM.render(
  <div>
    <App title="There is simple react + ts bundler" />
  </div>,
  document.getElementById("root")
);
